# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# browser-automation scratch (Playwright MCP snapshots, console logs, screenshots)
/.playwright-mcp/

# next.js
/.next/
/out/
# Finder-duplicated build folders (e.g. ".next 2", ".next 3")
/.next [0-9]*/
/out [0-9]*/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files — including the .example templates (owner decision 2026-07-16:
# these stay local-only, never tracked or pushed again; see note below)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.claude/setting.local.json
.codebase-memory/

# design-track working material (belongs on design/ui-pass)
Lomba Kita v2/

# Architecture/decision docs — local-only, intentionally untracked (DEC-0101)
# Purged from git history 2026-07-16; never re-commit or push these again
/.claude/
/CLAUDE.md
/docs/
/README.md
/AGENTS.md
/src/worker/README.md
